goals:
  sw:
    program:
      elf:
        kind: artifact
        public: true
        description: bad
        params:
        - program
        artifact:
          input_globs:
          - sw/programs/{missing}/*.c
          outputs:
            elf:
              description: bad output
              path: '{program}.elf'
